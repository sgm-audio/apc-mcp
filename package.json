{
  "name": "@sgm-audio/apc-mcp",
  "version": "1.5.0",
  "description": "MCP server for audio plugin development — CMake build, test, lint, and project management for JUCE, CLAP, VST3, and ARA plugin projects",
  "type": "module",
  "bin": {
    "apc-mcp": "./index.js"
  },
  "files": [
    "index.js",
    "templates/"
  ],
  "scripts": {
    "test": "node --test tests/server.test.js",
    "lint": "node --check index.js",
    "audit": "npm audit && echo '✅ No known vulnerabilities'",
    "quality": "node --check index.js && npm test",
    "security": "npm audit --audit-level=high && echo '✅ Security gate passed'",
    "check": "npm run quality && npm run audit",
    "ship": "npm run check && git tag v$npm_package_version && git push origin main --tags"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sgm-audio/apc-mcp.git"
  },
  "homepage": "https://github.com/sgm-audio/apc-mcp",
  "bugs": "https://github.com/sgm-audio/apc-mcp/issues",
  "license": "MIT",
  "keywords": [
    "mcp",
    "audio-plugin",
    "juce",
    "clap",
    "vst3",
    "cmake",
    "audio",
    "dsp",
    "modelcontextprotocol"
  ]
}
